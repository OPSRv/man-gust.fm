//components
import { PlayList } from "../generic/PlayList";
import { Slider } from "../slider/Slider";

const Content = () => {
  return (
    <>
      <Slider />
      <PlayList />
    </>
  );
};

export default Content;
